# AI CLI Tool Configuration
# Choose which AI CLI tool to use: 'gemini', 'kiro', or 'claude'
CLI_TOOL_TYPE=gemini

# Command to invoke the CLI tool
# For Gemini: 'gemini' (uses: gemini -p "prompt")
# For Kiro: 'kiro-cli' (uses: kiro-cli chat --no-interactive --trust-all-tools "prompt")
# For Claude Code: 'claude' (uses: claude -p "prompt" --output-format text)
# You can also specify full paths like '/usr/local/bin/gemini'
CLI_COMMAND=gemini

# Set to 'true' to use mock service (for testing without CLI tool)
USE_MOCK_GEMINI=true

# Twitter API Configuration (optional - for Twitter/X URL support)
# Get your Bearer Token from https://developer.twitter.com/en/portal/dashboard
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# Input Configuration
OBSIDIAN_NOTES_PATH=./notes

# Processing Options
# Set to 'true' to preview results without modifying files
DRY_RUN=false
